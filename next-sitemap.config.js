/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://cloudbd.vercel.app', // Replace with your website's URL
    generateRobotsTxt: true,           // (optional) Generate a robots.txt file
  };
  
  module.exports = config;
  